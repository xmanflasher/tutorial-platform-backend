# ==================================
# 1. ??????? (PostgreSQL)
# ==================================

# ?????: tutorial_db (?? docker-compose.yml)
# Host: localhost (?????? IntelliJ ??)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/tutorial_db}
#spring.datasource.url=jdbc:postgresql://localhost:5433/tutorial_db

# ???? (?? docker-compose.yml)
spring.datasource.username=postgres_user
spring.datasource.password=postgres_password

# ????
spring.datasource.driver-class-name=org.postgresql.Driver

# ==================================
# 2. JPA/Hibernate ??
# ==================================

# update: ????
spring.jpa.hibernate.ddl-auto=update

# ?? SQL
spring.jpa.show-sql=true

# ?????
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==================================
# 3. ????
# ==================================

spring.jackson.deserialization.fail-on-unknown-properties=false
server.port=8080

# ==================================
# 4. Security / OAuth2 ??
# ==================================

# Google (?????????????)
spring.security.oauth2.client.registration.google.client-id=temp-google-id
spring.security.oauth2.client.registration.google.client-secret=temp-google-secret
spring.security.oauth2.client.registration.google.scope=email,profile,openid
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# Facebook (?????)
spring.security.oauth2.client.registration.facebook.client-id=temp-facebook-id
spring.security.oauth2.client.registration.facebook.client-secret=temp-facebook-secret
spring.security.oauth2.client.registration.facebook.scope=email,public_profile
spring.security.oauth2.client.registration.facebook.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# ???????????? (?? 32 ???)??????????
jwt.secret-key=MySuperSecretKeyForDevTesting1234567890